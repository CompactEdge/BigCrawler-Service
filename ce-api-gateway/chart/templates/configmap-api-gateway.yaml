kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ .Values.configMapName }}
  namespace: "{{ .Release.namespace }}"
data:
{{- if .Values.configFiles }}
  ce-api-gateway.conf: |-
{{ .Files.Get "files/ce-api-gateway.conf" | indent 4 }}
{{ end }}
